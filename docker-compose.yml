services:
  procurement-agent:
    build:
      context: .
      dockerfile: procurement-agent/Dockerfile
    ports:
      - "8003:8000"
    restart: unless-stopped

  shared:
    build:
      context: .
      dockerfile: shared/Dockerfile
    ports:
      - "8000:8000"
    restart: unless-stopped

  # frontend:
  #   build: ./frontend
  #   ports:
  #     - "3000:3000"

  # risk-agent:
  #   build: ./risk-agent
  #   ports:
  #     - "8001:8000"

  scenario-engine:
    build:
      context: .
      dockerfile: scenario-engine/Dockerfile
    ports:
      - "8002:8000"

  spr-agent:
    build:
      context: .
      dockerfile: spr-agent/Dockerfile
    ports:
      - "8004:8000"

  # coordinator:
  #   build: ./coordinator
  #   ports:
  #     - "8005:8000"
