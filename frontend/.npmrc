# react-simple-maps@3 declares a peer of react 16/17/18, but this app runs
# react 19. The dependency works fine at runtime; this flag lets a clean
# `npm install` succeed instead of failing with ERESOLVE.
# TODO: remove once react-simple-maps is upgraded to a react-19-compatible release.
legacy-peer-deps=true
